<UserControl x:Class="Music_Player.Views.SongItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Music_Player.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="380">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding Name}" Style="{StaticResource SongItemViewLabel}"/>
        <Label Grid.Row="1" Content="{Binding Artist}" Style="{StaticResource SongItemViewLabel}"/>
        <Label Grid.Row="2" Content="{Binding Genre}" Style="{StaticResource SongItemViewLabel}"/>

    </Grid>
</UserControl>
